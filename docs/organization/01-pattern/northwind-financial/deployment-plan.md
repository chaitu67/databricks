# Deployment plan: northwind-financial

Generated by `6.3-generate-deployment-plan` from [`databricks-mapping.md`](databricks-mapping.md).

**State check performed**: no `pattern01_units.auto.tfvars` entries exist for any of
`northwind-financial`'s unit keys yet — every line below is new. Unlike when
`meridian-financial` was first planned, `01-pattern`'s mechanism itself is already built and
validated (no "Item 0" engineering prerequisite blocks anything here).

## Summary

- **New workspaces: 20** (14 catalog-bearing + 6 DR-only standbys in us-west-2) — blast radius
  `new real infra` (VPC/S3/IAM per the underlying `modules/workspace`) on every one. Confirm the
  full 20-workspace scale is actually wanted before starting — scoping down to one BU end-to-end
  first (and/or deferring the DR standbys) is a legitimate alternative.
- **New catalogs: 22**, **new groups: 66** (3 per catalog) — blast radius `new real storage`
  (catalogs) / `no new infra` (groups). The 6 DR standby workspaces get **no catalogs, ever** —
  see `databricks-mapping.md` Section 2a.
- **Cross-BU grant entries: 34** (`platform_it` ×16, `finance_ops` ×12, `risk_compliance` ×6) —
  all currently assumed **read-only**, per the mapping doc's open assumption (not stated during
  discovery). Confirm before submitting any of these.

## Ordered checklist (BUs first — dev → stg → prod — then shared departments, whose cross-grants depend on the BUs' catalogs)

### retail

- [x] Workspace `retail-dev` (`add-pattern01-unit.sh retail_dev`). PR:
      https://github.com/chaitu67/databricks/pull/15 (workspace-only, awaiting review/merge/apply).
  - [ ] Catalog `dev_retail_brokerage` + groups + grants. Waiting on the workspace above merging,
        applying, and reporting `RUNNING` (via `08-lob-setup`, once that's confirmed).
  - [ ] Catalog `dev_retail_advisory` + groups + grants. Same dependency.
  - **Stop point**: review both catalogs' plan/apply before `retail-prod`.
- [ ] Workspace `retail-prod` (`add-pattern01-unit.sh retail_prod`). Depends on: `retail-dev`
      reviewed.
  - [ ] Catalog `prod_retail_brokerage` + groups + grants.
  - [ ] Catalog `prod_retail_advisory` + groups + grants.
  - **Stop point**: explicit prod sign-off required.
- [ ] DR standby workspace `retail-prod-dr` (`add-pattern01-unit.sh retail_prod_dr`), region
      `us-west-2`, `catalogs = {}` **permanently** — no follow-up catalogs PR for this one, ever.
  - **Stop point**: explicit prod sign-off required (same real-infra cost as any workspace).

### wealth

- [ ] Workspace `wealth-dev` (`add-pattern01-unit.sh wealth_dev`).
  - [ ] Catalog `dev_wealth_private_banking` + groups + grants.
  - [ ] Catalog `dev_wealth_trust_services` + groups + grants.
  - **Stop point**: review before `wealth-stg`.
- [ ] Workspace `wealth-stg` (`add-pattern01-unit.sh wealth_stg`). Depends on: `wealth-dev`
      reviewed.
  - [ ] Catalog `stg_wealth_private_banking` + groups + grants.
  - [ ] Catalog `stg_wealth_trust_services` + groups + grants.
  - **Stop point**: review before `wealth-prod`.
- [ ] Workspace `wealth-prod` (`add-pattern01-unit.sh wealth_prod`). Depends on: `wealth-stg`
      reviewed.
  - [ ] Catalog `prod_wealth_private_banking` + groups + grants.
  - [ ] Catalog `prod_wealth_trust_services` + groups + grants.
  - **Stop point**: explicit prod sign-off required.
- [ ] DR standby workspace `wealth-prod-dr` (`add-pattern01-unit.sh wealth_prod_dr`), region
      `us-west-2`, `catalogs = {}` **permanently** — no follow-up catalogs PR for this one, ever.
  - **Stop point**: explicit prod sign-off required (same real-infra cost as any workspace).

### institutional

- [ ] Workspace `institutional-dev` (`add-pattern01-unit.sh institutional_dev`).
  - [ ] Catalog `dev_institutional_asset_management` + groups + grants.
  - [ ] Catalog `dev_institutional_pension_services` + groups + grants.
  - **Stop point**: review before `institutional-stg`.
- [ ] Workspace `institutional-stg` (`add-pattern01-unit.sh institutional_stg`). Depends on:
      `institutional-dev` reviewed.
  - [ ] Catalog `stg_institutional_asset_management` + groups + grants.
  - [ ] Catalog `stg_institutional_pension_services` + groups + grants.
  - **Stop point**: review before `institutional-prod`.
- [ ] Workspace `institutional-prod` (`add-pattern01-unit.sh institutional_prod`). Depends on:
      `institutional-stg` reviewed.
  - [ ] Catalog `prod_institutional_asset_management` + groups + grants.
  - [ ] Catalog `prod_institutional_pension_services` + groups + grants.
  - **Stop point**: explicit prod sign-off required.
- [ ] DR standby workspace `institutional-prod-dr`
      (`add-pattern01-unit.sh institutional_prod_dr`), region `us-west-2`, `catalogs = {}`
      **permanently** — no follow-up catalogs PR for this one, ever.
  - **Stop point**: explicit prod sign-off required (same real-infra cost as any workspace).

### platform_it

- [ ] Workspace `platform_it-dev` (`add-pattern01-unit.sh platform_it_dev`).
  - [ ] Catalog `dev_platform_it` + groups + grants.
  - **Stop point**: review before `platform_it-stg`.
- [ ] Workspace `platform_it-stg` (`add-pattern01-unit.sh platform_it_stg`). Depends on:
      `platform_it-dev` reviewed.
  - [ ] Catalog `stg_platform_it` + groups + grants.
  - **Stop point**: review before `platform_it-prod`.
- [ ] Workspace `platform_it-prod` (`add-pattern01-unit.sh platform_it_prod`). Depends on:
      `platform_it-stg` reviewed.
  - [ ] Catalog `prod_platform_it` + groups + grants.
  - **Stop point**: explicit prod sign-off required.
- [ ] DR standby workspace `platform_it-prod-dr` (`add-pattern01-unit.sh platform_it_prod_dr`),
      region `us-west-2`, `catalogs = {}` **permanently** — no follow-up catalogs PR for this one,
      ever.
  - **Stop point**: explicit prod sign-off required (same real-infra cost as any workspace).
- Cross-BU reads (`extra_grants` on each target unit's own module call — depend on that target
  catalog already applied; **read-only assumption**, confirm before submitting):
  - [ ] Grant `acl_dev_platform_it_reader` read-only on `dev_retail_brokerage`.
  - [ ] Grant `acl_prod_platform_it_reader` read-only on `prod_retail_brokerage`.
  - [ ] Grant `acl_dev_platform_it_reader` read-only on `dev_retail_advisory`.
  - [ ] Grant `acl_prod_platform_it_reader` read-only on `prod_retail_advisory`.
  - [ ] Grant `acl_dev_platform_it_reader` read-only on `dev_wealth_private_banking`.
  - [ ] Grant `acl_stg_platform_it_reader` read-only on `stg_wealth_private_banking`.
  - [ ] Grant `acl_prod_platform_it_reader` read-only on `prod_wealth_private_banking`.
  - [ ] Grant `acl_dev_platform_it_reader` read-only on `dev_wealth_trust_services`.
  - [ ] Grant `acl_stg_platform_it_reader` read-only on `stg_wealth_trust_services`.
  - [ ] Grant `acl_prod_platform_it_reader` read-only on `prod_wealth_trust_services`.
  - [ ] Grant `acl_dev_platform_it_reader` read-only on `dev_institutional_asset_management`.
  - [ ] Grant `acl_stg_platform_it_reader` read-only on `stg_institutional_asset_management`.
  - [ ] Grant `acl_prod_platform_it_reader` read-only on `prod_institutional_asset_management`.
  - [ ] Grant `acl_dev_platform_it_reader` read-only on `dev_institutional_pension_services`.
  - [ ] Grant `acl_stg_platform_it_reader` read-only on `stg_institutional_pension_services`.
  - [ ] Grant `acl_prod_platform_it_reader` read-only on `prod_institutional_pension_services`.

### risk_compliance

- [ ] Workspace `risk_compliance-prod` (`add-pattern01-unit.sh risk_compliance_prod`; no lower
      tier, per `org-structure.yaml`).
  - [ ] Catalog `prod_risk_compliance` + groups + grants.
  - **Stop point**: explicit prod sign-off required.
- [ ] DR standby workspace `risk_compliance-prod-dr`
      (`add-pattern01-unit.sh risk_compliance_prod_dr`), region `us-west-2`, `catalogs = {}`
      **permanently** — no follow-up catalogs PR for this one, ever.
  - **Stop point**: explicit prod sign-off required (same real-infra cost as any workspace).
- Cross-BU reads (**read-only assumption**, confirm before submitting):
  - [ ] Grant `acl_prod_risk_compliance_reader` read-only on `prod_retail_brokerage`.
  - [ ] Grant `acl_prod_risk_compliance_reader` read-only on `prod_retail_advisory`.
  - [ ] Grant `acl_prod_risk_compliance_reader` read-only on `prod_wealth_private_banking`.
  - [ ] Grant `acl_prod_risk_compliance_reader` read-only on `prod_wealth_trust_services`.
  - [ ] Grant `acl_prod_risk_compliance_reader` read-only on `prod_institutional_asset_management`.
  - [ ] Grant `acl_prod_risk_compliance_reader` read-only on `prod_institutional_pension_services`.

### finance_ops

- [ ] Workspace `finance_ops-dev` (`add-pattern01-unit.sh finance_ops_dev`).
  - [ ] Catalog `dev_finance_ops` + groups + grants.
  - **Stop point**: review before `finance_ops-prod`.
- [ ] Workspace `finance_ops-prod` (`add-pattern01-unit.sh finance_ops_prod`). Depends on:
      `finance_ops-dev` reviewed.
  - [ ] Catalog `prod_finance_ops` + groups + grants.
  - **Stop point**: explicit prod sign-off required.
- [ ] DR standby workspace `finance_ops-prod-dr` (`add-pattern01-unit.sh finance_ops_prod_dr`),
      region `us-west-2`, `catalogs = {}` **permanently** — no follow-up catalogs PR for this one,
      ever.
  - **Stop point**: explicit prod sign-off required (same real-infra cost as any workspace).
- Cross-BU reads (`stg` skipped — `finance_ops` doesn't operate there; **read-only assumption**,
  confirm before submitting):
  - [ ] Grant `acl_dev_finance_ops_reader` read-only on `dev_retail_brokerage`.
  - [ ] Grant `acl_prod_finance_ops_reader` read-only on `prod_retail_brokerage`.
  - [ ] Grant `acl_dev_finance_ops_reader` read-only on `dev_retail_advisory`.
  - [ ] Grant `acl_prod_finance_ops_reader` read-only on `prod_retail_advisory`.
  - [ ] Grant `acl_dev_finance_ops_reader` read-only on `dev_wealth_private_banking`.
  - [ ] Grant `acl_prod_finance_ops_reader` read-only on `prod_wealth_private_banking`.
  - [ ] Grant `acl_dev_finance_ops_reader` read-only on `dev_wealth_trust_services`.
  - [ ] Grant `acl_prod_finance_ops_reader` read-only on `prod_wealth_trust_services`.
  - [ ] Grant `acl_dev_finance_ops_reader` read-only on `dev_institutional_asset_management`.
  - [ ] Grant `acl_prod_finance_ops_reader` read-only on `prod_institutional_asset_management`.
  - [ ] Grant `acl_dev_finance_ops_reader` read-only on `dev_institutional_pension_services`.
  - [ ] Grant `acl_prod_finance_ops_reader` read-only on `prod_institutional_pension_services`.

## Handoff reminder

Nothing above has been applied. Executing this plan means working through the checklist top to
bottom via `6.4-deploy-organization` — one PR per unit (workspace-only first, catalogs as a
follow-up PR once each workspace is `RUNNING`), submitted through `4.5-open-terraform-pr`, always
a human-reviewed `terraform plan` before merge/apply.

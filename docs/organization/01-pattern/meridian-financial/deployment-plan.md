# Deployment plan: meridian-financial

Generated by `6.3-generate-deployment-plan` from
[`databricks-mapping.md`](databricks-mapping.md). **Regenerated** for the canonical
workspace-per-unit×environment / LOB-as-catalog pattern.

**State check performed**: compared against the real `databricks/infrastructure/*.auto.tfvars`
files. The existing `workshop-workspace` and its `analytics` catalog/groups are unrelated to this
org model and untouched. Every item below is new.

## Summary

- **New workspaces: 18** — blast radius `new real infra` (VPC/S3/IAM per `5.1`) on every one.
  This is the dominant cost driver in this plan; get explicit sign-off on the full count before
  starting, not just per item.
- **New catalogs: 27**, **new groups: 81** (3 per catalog), **catalog-level grants: 27** (one
  triad per catalog, bundled with its catalog line below) — blast radius `new real storage`
  (catalogs) / `no new infra` (groups, grants).
- **Cross-BU grant edits: 20** (`risk_compliance` ×8, `finance_ops` ×8, `investments` ×4).

## Item 0 — blocking prerequisite (must land before any catalog beyond the first workspace)

- [ ] **Extend `05-databricks-terraform-deployment`'s Terraform project with a
      provider-per-workspace pattern for catalogs** — a distinct, aliased `databricks` provider
      per workspace, plus catalog/schema/group/grant resource wiring against each alias. This is
      an engineering change to `05` itself, not a per-unit plan item. Until it lands, only one
      workspace total can have Terraform-managed catalogs (currently `workshop-workspace`, already
      used by the unrelated `analytics` catalog) — every catalog item below is **planned, not
      executable**, regardless of checklist order.

## Ordered checklist (per unit: workspace(s) → its LOB catalogs → groups/grants, dev → stg → prod)

### retail

- [ ] Workspace `retail-dev` (`5.1-create-workspace`). Depends on: Item 0 landed (for its
      catalogs) — the workspace itself can be created independently of Item 0, only its catalogs
      are blocked.
  - [ ] Catalog `dev_retail_brokerage` + `acl_dev_retail_brokerage_reader/writer/owner` + grants
        (`5.2` then `5.3`).
  - [ ] Catalog `dev_retail_advisory` + `acl_dev_retail_advisory_reader/writer/owner` + grants.
  - **Stop point**: review both catalogs' plan/apply before `retail-stg`.
- [ ] Workspace `retail-stg`. Depends on: `retail-dev` reviewed.
  - [ ] Catalog `stg_retail_brokerage` + groups + grants.
  - [ ] Catalog `stg_retail_advisory` + groups + grants.
  - **Stop point**: review before `retail-prod`.
- [ ] Workspace `retail-prod`. Depends on: `retail-stg` reviewed.
  - [ ] Catalog `prod_retail_brokerage` + groups + grants.
  - [ ] Catalog `prod_retail_advisory` + groups + grants.
  - Open question: PII masking on both LOBs not enforceable yet (abac not built) — confirm
        restricted-membership mitigation is acceptable.
  - **Stop point**: explicit prod sign-off required.

### fas

- [ ] Workspace `fas-dev`.
  - [ ] Catalog `dev_fas_distribution` + groups + grants.
  - [ ] Catalog `dev_fas_model_portfolios` + groups + grants.
  - **Stop point**: review before `fas-prod`.
- [ ] Workspace `fas-prod`. Depends on: `fas-dev` reviewed.
  - [ ] Catalog `prod_fas_distribution` + groups + grants.
  - [ ] Catalog `prod_fas_model_portfolios` + groups + grants.
  - **Stop point**: explicit prod sign-off required.

### institutional

- [ ] Workspace `institutional-dev`.
  - [ ] Catalog `dev_institutional_retirement` + groups + grants.
  - [ ] Catalog `dev_institutional_institutional_am` + groups + grants.
  - **Stop point**: review before `institutional-prod`.
- [ ] Workspace `institutional-prod`. Depends on: `institutional-dev` reviewed.
  - [ ] Catalog `prod_institutional_retirement` + groups + grants. Open question: PII masking on
        participant records, same gap as retail.
  - [ ] Catalog `prod_institutional_institutional_am` + groups + grants.
  - **Stop point**: explicit prod sign-off required.

### international

- [ ] Workspace `international-dev`. Open question: confirm this unit's environment tiers with
      the user before running — `org-structure.yaml` marked them inferred, not stated.
  - [ ] Catalog `dev_international_uk_eu` + groups + grants.
  - [ ] Catalog `dev_international_apac` + groups + grants.
  - **Stop point**: review before `international-prod`, and confirm the open question above.
- [ ] Workspace `international-prod`. Depends on: `international-dev` reviewed.
  - [ ] Catalog `prod_international_uk_eu` + groups + grants.
  - [ ] Catalog `prod_international_apac` + groups + grants.
  - **Stop point**: explicit prod sign-off required.

### platform_it

- [ ] Workspace `platform_it-dev`.
  - [ ] Catalog `dev_platform_it` + groups + grants.
  - **Stop point**: review before `platform_it-stg`.
- [ ] Workspace `platform_it-stg`. Depends on: `platform_it-dev` reviewed.
  - [ ] Catalog `stg_platform_it` + groups + grants.
  - **Stop point**: review before `platform_it-prod`.
- [ ] Workspace `platform_it-prod`. Depends on: `platform_it-stg` reviewed.
  - [ ] Catalog `prod_platform_it` + groups + grants.
  - **Stop point**: explicit prod sign-off required.

### risk_compliance

- [ ] Workspace `risk_compliance-dev`.
  - [ ] Catalog `dev_risk_compliance` + groups + grants.
  - **Stop point**: review before `risk_compliance-prod`.
- [ ] Workspace `risk_compliance-prod`. Depends on: `risk_compliance-dev` reviewed.
  - [ ] Catalog `prod_risk_compliance` + groups + grants.
  - **Stop point**: explicit prod sign-off required.
- Cross-BU reads (depend on every target catalog above already applied):
  - [ ] Grant `acl_prod_risk_compliance_reader` read-only on `prod_retail_brokerage`.
  - [ ] Grant `acl_prod_risk_compliance_reader` read-only on `prod_retail_advisory`.
  - [ ] Grant `acl_prod_risk_compliance_reader` read-only on `prod_fas_distribution`.
  - [ ] Grant `acl_prod_risk_compliance_reader` read-only on `prod_fas_model_portfolios`.
  - [ ] Grant `acl_prod_risk_compliance_reader` read-only on `prod_institutional_retirement`.
  - [ ] Grant `acl_prod_risk_compliance_reader` read-only on `prod_institutional_institutional_am`.
  - [ ] Grant `acl_prod_risk_compliance_reader` read-only on `prod_international_uk_eu`.
  - [ ] Grant `acl_prod_risk_compliance_reader` read-only on `prod_international_apac`.

### finance_ops

- [ ] Workspace `finance_ops-dev`.
  - [ ] Catalog `dev_finance_ops` + groups + grants.
  - **Stop point**: review before `finance_ops-prod`.
- [ ] Workspace `finance_ops-prod`. Depends on: `finance_ops-dev` reviewed.
  - [ ] Catalog `prod_finance_ops` + groups + grants.
  - **Stop point**: explicit prod sign-off required.
- Cross-BU reads (same 8 target catalogs as `risk_compliance`, depend on those already applied):
  - [ ] Grant `acl_prod_finance_ops_reader` read-only on `prod_retail_brokerage`.
  - [ ] Grant `acl_prod_finance_ops_reader` read-only on `prod_retail_advisory`.
  - [ ] Grant `acl_prod_finance_ops_reader` read-only on `prod_fas_distribution`.
  - [ ] Grant `acl_prod_finance_ops_reader` read-only on `prod_fas_model_portfolios`.
  - [ ] Grant `acl_prod_finance_ops_reader` read-only on `prod_institutional_retirement`.
  - [ ] Grant `acl_prod_finance_ops_reader` read-only on `prod_institutional_institutional_am`.
  - [ ] Grant `acl_prod_finance_ops_reader` read-only on `prod_international_uk_eu`.
  - [ ] Grant `acl_prod_finance_ops_reader` read-only on `prod_international_apac`.

### hr

- [ ] Workspace `hr-prod` (no lower tier stated).
  - [ ] Catalog `prod_hr` + groups + grants.
  - **Stop point**: explicit prod sign-off required.

### investments

- [ ] Workspace `investments-prod`. Note: this workspace structurally satisfies the org model's
      stated air-gapped/isolated-compute requirement — confirm its actual network/cluster-policy
      configuration meets whatever standard prompted that requirement (a `5.1`-time detail).
  - [ ] Catalog `prod_investments` + groups + grants.
  - **Stop point**: explicit prod sign-off required.
- Cross-BU reads (scope is `[retail, institutional]` only, depend on those already applied):
  - [ ] Grant `acl_prod_investments_reader` read-only on `prod_retail_brokerage`.
  - [ ] Grant `acl_prod_investments_reader` read-only on `prod_retail_advisory`.
  - [ ] Grant `acl_prod_investments_reader` read-only on `prod_institutional_retirement`.
  - [ ] Grant `acl_prod_investments_reader` read-only on `prod_institutional_institutional_am`.

## Handoff reminder

Nothing above has been applied, and Item 0 blocks every catalog item regardless of checklist
position. Executing this plan means: land Item 0 first, then work the remaining checklist top to
bottom, running the named `05.N-*` skill's own Phase 1/2/3 for each item — including its own
human-reviewed `terraform plan` before every `apply`. This plan only supplies *what* and *in what
order*; `05`'s existing discipline still governs *how*.

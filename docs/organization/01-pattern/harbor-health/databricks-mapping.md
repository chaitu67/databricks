# Databricks mapping: harbor-health

Generated by `6.2-map-databricks-pattern` from [`org-structure.yaml`](org-structure.yaml).

## 1. Pattern used

**[01-pattern](../pattern-definition.md)** — one workspace per business unit/department ×
environment tier; a line of business (LOB) is a catalog inside its unit's workspace. Fits this
org: a single BU (`pharmacy`), no LOB split, no cross-unit access, no sensitivity/isolation flags
that would require a different shape — the simplest possible instance of `01-pattern`, same
mechanism as `meridian-financial`/`northwind-financial`, just with one unit instead of several.

Realized by `infrastructure/modules/organization/01-pattern` (self-contained per-unit provider),
scaffolded via `infrastructure/scripts/add-pattern01-unit.sh pharmacy`. This mechanism is already
built and validated (see `01-pattern/pattern-definition.md` — Status) — no outstanding engineering
prerequisite blocks this.

## 2. Workspaces and scale

**1 workspace** — `pharmacy` has only `dev` in scope (`org-structure.yaml`), so there is exactly
one (unit × environment) pair.

| Unit | Environments | Workspace (`<org>_<bu>_<env>`, `naming-conventions.md`) |
|---|---|---|
| pharmacy | dev | `harbor_health_pharmacy_dev` |

Deploys to **us-east-1** — `org-structure.yaml` scopes the whole org to that single region (no
`environment_regions` map needed, per `01-pattern`'s rule for single-region orgs). No `dr` block
was stated, so no DR standby workspace.

This is real, billed AWS infrastructure (VPC/S3/IAM per `modules/workspace`) — just one workspace
in this case, not a large commitment.

## 3. Catalogs, schemas, and groups

Schema layout: `bronze`/`silver`/`gold` (this pattern's default — no data-sensitivity override was
stated for this org).

| Unit | LOB | Env | Catalog | Groups |
|---|---|---|---|---|
| pharmacy | — (no LOB split) | dev | `dev_pharmacy` | `acl_dev_pharmacy_reader/writer/owner` |

**1 catalog, 3 groups** (3 roles × 1 catalog).

No PII/sensitivity flags were stated for this org — no open masking gaps to carry forward.

## 4. Cross-BU grants

None — no shared/functional departments were stated in `org-structure.yaml`, and there's only one
business unit, so there is nothing to grant across.

## 5. Volumes

No volume (raw file / landing-zone storage) requirements captured in `org-structure.yaml`. None
proposed here.

## 6. Constraints carried forward from `01-pattern`

- **Two-phase requirement**: `pharmacy-dev`'s workspace must be created and confirmed `RUNNING`
  before its `dev_pharmacy` catalog can be added — the catalog's provider needs the workspace's
  real, Databricks-assigned URL. This is a brand-new unit, so the one-time
  `add-pattern01-unit.sh pharmacy` scaffold is required before any of this becomes a pure tfvars
  edit. See `pattern-definition.md` / `6.4-deploy-organization`'s "two-phase reality."
- **No volume support, no workspace-login/permission assignment for the groups created, no
  row/column masking** — same gaps as this pattern's other instances; the login-assignment gap
  still applies to the 3 groups created here.
- **This project's automation is AWS-only** — consistent with `org-structure.yaml` scoping this
  org to `us-east-1`.
- Small footprint by design (1 workspace, 1 catalog, 3 groups) — no scale concern to raise here,
  unlike the larger multi-BU orgs modeled under this pattern.

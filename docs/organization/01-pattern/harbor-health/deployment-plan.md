# Deployment plan: harbor-health

Generated by `6.3-generate-deployment-plan` from [`databricks-mapping.md`](databricks-mapping.md).

**State check performed**: no `pattern01_units.auto.tfvars` entries exist yet, and no
`pattern01_units_pharmacy.tf` scaffold file exists — every line below is new. `01-pattern`'s
mechanism itself is already built and validated, so no engineering prerequisite blocks anything
here.

## Summary

- **New workspaces: 1** (`pharmacy-dev`) — blast radius `new real infra` (VPC/S3/IAM per
  `modules/workspace`).
- **New catalogs: 1** (`dev_pharmacy`), **new groups: 3** — blast radius `new real storage`
  (catalog) / `no new infra` (groups).
- **Cross-BU grant entries: 0** — no shared departments, single BU.

## Ordered checklist

### pharmacy

- [ ] Workspace `pharmacy-dev` (`add-pattern01-unit.sh pharmacy_dev`). Blast radius: `new real
      infra`.
  - **Stop point**: confirm workspace is `RUNNING` before submitting the catalog PR (two-phase
    requirement — the catalog's provider needs the workspace's real, Databricks-assigned URL).
  - [ ] Catalog `dev_pharmacy` + groups (`acl_dev_pharmacy_reader/writer/owner`) + grants. Blast
        radius: `new real storage` (catalog) / `no new infra` (groups). Depends on: `pharmacy-dev`
        confirmed `RUNNING`.
  - **Stop point**: this org has only `dev` in scope — no `stg`/`prod` tier to gate before this
    plan is considered complete.

## Handoff reminder

Nothing above has been applied. Executing this plan means working through the checklist via
`6.4-deploy-organization` — one PR per unit of change (workspace first, catalog as a follow-up PR
once the workspace is `RUNNING`), submitted through `4.5-open-terraform-pr`, always a
human-reviewed `terraform plan` before merge/apply.
